name: app_screenshots_cli
description: CLI client to remote-control the App Screenshots desktop app.
version: 1.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  app_screenshots_shared:
    path: ../app_screenshots_shared
  args: ^2.4.0
  http: ^1.2.0

dev_dependencies:
  test: ^1.24.0

executables:
  appshots: appshots
