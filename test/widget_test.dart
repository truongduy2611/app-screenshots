import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — widget and integration tests to be added
  // as the Screenshot Studio feature is built out.
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
